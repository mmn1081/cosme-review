<%= form_with model: post_cosme.comments.new, url:  public_post_cosme_comments_path(post_cosme), local: false do |f| %><!--jsにするためにlocal: falseと記述　HTMLはlocal: trueと記述-->
  <%= f.text_area :comment, rows: '5',placeholder: "コメントをここに", class: "w-100 comment-textarea" %>
  <%= f.submit "送信" %>
<% end %>